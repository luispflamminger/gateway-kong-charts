{{- if eq .Values.rbac.enabled true }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  labels: {{ include "kong.labels" $ | nindent 4 }}
type: Opaque
data:
  kongAdminPassword: {{ (.Values.rbac.kongAdminPassword | default "superuser") | b64enc }}
{{- end -}}