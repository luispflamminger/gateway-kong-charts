{{- if eq .Values.database.location "local" -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-database-pvc
  labels: {{ include "postgresql.labels" $ | nindent 4 }}
{{- if .Values.database.postgres.persistence }}
  {{- if .Values.database.postgres.persistence.keepOnDelete }}
  annotations:
    "helm.sh/resource-policy": keep
  {{- end -}}
{{- end }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: {{ .Values.global.storageclass | default "cns" }}
  resources: {{ .Values.database.postgres.persistence.resources | toYaml | nindent 4 }}
{{- end -}}