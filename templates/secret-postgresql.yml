apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-postgresql
  labels: {{ include "postgresql.labels" $ | nindent 4 }}
type: Opaque
data:
  password: {{ (.Values.postgres.password | default "kong") | b64enc }}